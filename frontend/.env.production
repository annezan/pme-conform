# Production : le front React est servi par Laravel sur le MEME domaine que l'API.
# On utilise donc des URLs relatives — le navigateur cible automatiquement le
# domaine courant (ns3485498.ip-193-70-32.eu aujourd'hui, un vrai domaine demain,
# sans rebuild). C'est aussi requis par l'auth Sanctum (cookies same-origin).
VITE_API_URL=/api
VITE_BACKEND_URL=
